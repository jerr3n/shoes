# Shoes
*A voodoo-filled socket library that doesn't use long-polling for Roblox!*

---

notes:

genuinely just use datagrip to handle that stupid sqlite db

```sqlite
SELECT * FROM API_KEYS;
DELETE FROM API_KEYS WHERE API_KEY NOT NULL;
```